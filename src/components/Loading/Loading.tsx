import { LoadingWrapper, LoadingDots } from "./Loading.styles";

const Loading: React.FC = () => {
  return (
    <LoadingWrapper>
      <LoadingDots />
    </LoadingWrapper>
  );
};

export default Loading;
